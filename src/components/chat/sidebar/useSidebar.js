// //at here we will handle logic when the user click a button all or room to filter the conversation list
// import {useEffect, useState} from "react";

// export function useSidebar(onFilterChange) {
//     const handleClick = (e) => {
//         const buttonValue = e.target.textContent.value;
//         onFilterChange(buttonValue);
//     }

// };